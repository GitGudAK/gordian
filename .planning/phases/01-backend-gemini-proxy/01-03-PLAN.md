---
phase: 01-backend-gemini-proxy
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - proxy/scripts/bake-off.mts
  - proxy/package.json
  - proxy/wrangler.jsonc  # (conditional — only if the Gemini model name 404s and the vars flip to gemini-flash-latest)
autonomous: false
requirements: [REQ-001, REQ-006]
user_setup:
  - service: cloudflare
    why: "Workers hosting — account, wrangler login, and secret storage are operator-only actions"
    dashboard_config:
      - task: "Create free Cloudflare account (if none) and authorize wrangler via browser OAuth"
        location: "dash.cloudflare.com signup, then `npx wrangler login` in the operator's terminal"
  - service: gemini
    why: "Upstream LLM key — the spike-era key is compromised (pasted in chat) and MUST be replaced"
    env_vars:
      - name: GEMINI_API_KEY
        source: "aistudio.google.com → API keys → create NEW key; set via `npx wrangler secret put GEMINI_API_KEY` (operator terminal only — Claude never handles the value)"
  - service: anthropic
    why: "Bake-off only — DEFERRED; adapter ships mock-tested, key set whenever the bake-off runs"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → API keys (later; not launch-blocking)"

must_haves:
  truths:
    - "A live workers.dev URL serves POST /v1/session-plan and POST /v1/verdict returning schema-valid JSON from real Gemini"
    - "GEMINI_API_KEY is stored as a write-only Worker secret, set by the operator — Claude never sees the value"
    - "curl without X-Device-ID returns 400 with error.code bad_request against the deployed Worker"
    - "GET /v1/guides on the deployed Worker returns 404 (reserved route)"
    - "The bake-off script runs the 20-dilemma matrix through both providers when keys exist, and skips loudly with exit 0 when they don't"
  artifacts:
    - path: "proxy/scripts/bake-off.mts"
      provides: "Same-20-dilemmas latency+quality comparison across MODEL_SESSION_PLAN/MODEL_VERDICT candidates, importing the real adapters"
      contains: "claude-haiku-4-5"
    - path: "proxy/package.json"
      provides: "bake-off npm script entry"
      contains: "bake-off"
  key_links:
    - from: "proxy/scripts/bake-off.mts"
      to: "proxy/src/providers/gemini.ts"
      via: "direct import (Node type-stripping) — exercises the exact production adapter code"
      pattern: "providers/gemini"
    - from: "proxy/scripts/live-check.mjs"
      to: "deployed workers.dev URL"
      via: "BASE_URL env var"
      pattern: "BASE_URL"
---

<objective>
Ship the Worker built in plans 01-01 and 01-02: write the model bake-off script (runnable later when ANTHROPIC_API_KEY exists), have the operator set up Cloudflare auth and a FRESH Gemini key (the spike key is compromised), deploy to workers.dev, and smoke both endpoints live with curl plus the live-check script.

Purpose: completes REQ-001 (a deployed first-party service holding the key — zero user setup) and proves REQ-006's error-code contract against the real deployment. The bake-off script satisfies the locked business hook: models swappable server-side per operation, Gemini flash vs Claude Haiku 4.5 (hybrid Sonnet-verdict option included).
Output: live `https://gordian-proxy.<subdomain>.workers.dev`, secrets set, bake-off tooling committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

Working directory for all commands: the gordian repo root (`/Users/ashwink/Desktop/Claude Projects/gordian`), Worker in `proxy/`.

@.planning/phases/01-backend-gemini-proxy/01-01-SUMMARY.md
@.planning/phases/01-backend-gemini-proxy/01-02-SUMMARY.md
@.planning/phases/01-backend-gemini-proxy/01-RESEARCH.md
@proxy/src/providers/types.ts
@proxy/src/operations.ts

<interfaces>
From plans 01-01/01-02 (use directly, no exploration): the Provider interface `generateStructured(req, env) → Promise<string>` in proxy/src/providers/types.ts; operation prompts/schemas exported from proxy/src/operations.ts; providers and operations are importable from plain Node (no workerd-specific imports — a stated 01-01 constraint). Model config var format: `"{provider}:{model}"`. Verified model IDs (research 2026-07-20): `gemini:gemini-3.5-flash` (fallback `gemini-flash-latest` on 404), `anthropic:claude-haiku-4-5`, `anthropic:claude-sonnet-5` (hybrid verdict candidate). The live smoke script proxy/scripts/live-check.mjs (from plan 01-02) is reused here against the deployed URL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model bake-off script (gemini vs anthropic, 20 dilemmas, latency + quality table)</name>
  <files>proxy/scripts/bake-off.mts, proxy/package.json</files>
  <read_first>
    - proxy/src/providers/types.ts, proxy/src/providers/gemini.ts, proxy/src/providers/anthropic.ts (adapter signatures to import)
    - proxy/src/operations.ts (prompt builders + schemas to reuse — do NOT duplicate prompt text in the script)
    - ios/Gordian/SessionViewModel.swift lines 494-526 (demo scenarios and canned rapid-fire answer shapes to reuse as bake-off inputs)
    - .planning/phases/01-backend-gemini-proxy/01-RESEARCH.md (section "Provider Adapter Design" — bake-off models and pricing)
  </read_first>
  <action>
    Write `proxy/scripts/bake-off.mts`, run with `node scripts/bake-off.mts` (Node ≥22 native type stripping; machine has v25 — no tsx/build step). It imports the real Gemini and Anthropic adapters plus the operation prompt builders/schemas from src/, passing an env object built from process.env — so the bake-off exercises the exact production adapter code, not a reimplementation.

    Inputs: 20 dilemmas defined inline — at least 8 BINARY-style (two named alternatives, e.g. "Should I learn Spanish or German?") and at least 8 YES_NO-style (go/no-go, e.g. "Should I quit my job to freelance?"); include the four demo scenarios from SessionViewModel.swift. For the verdict operation, pair each dilemma with 4-6 canned rapid-fire answers shaped like startDemoVerdict's (question/choice/reflection, some reflections empty).

    Model matrix (CLI-overridable via a --models argument accepting comma-separated "{provider}:{model}" entries; defaults): session-plan → gemini:gemini-3.5-flash and anthropic:claude-haiku-4-5; verdict → gemini:gemini-3.5-flash, anthropic:claude-haiku-4-5, and anthropic:claude-sonnet-5 (the hybrid Haiku-questions/Sonnet-verdict option from b2c-strategy).

    Env gating (this script must be runnable LATER, when ANTHROPIC_API_KEY exists): reads GEMINI_API_KEY and ANTHROPIC_API_KEY from process.env; a provider with no key is skipped with a loud per-provider notice; if NO keys are present at all, print a skip message naming both env vars and exit 0. A --limit N flag runs only the first N dilemmas (cheap trial runs).

    Output: a markdown table to stdout AND written to proxy/scripts/bake-off-results/{ISO-timestamp}.md (directory gitignored in 01-01). Row per dilemma × operation × model with columns: dilemma #, op, model, latency_ms, valid_json (did the payload parse with all required fields), mode classified (session-plan only), question_count and max question word-count (session-plan), decision snippet + decision word-count ≤15 check (verdict). Footer summary per model: median latency, parse success rate, spec-compliance rate. Quality judgment beyond these mechanical checks is the operator's call — the table is the raw material.

    Add `"bake-off": "node scripts/bake-off.mts"` to proxy/package.json scripts.
  </action>
  <verify>
    <automated>cd proxy && env -u GEMINI_API_KEY -u ANTHROPIC_API_KEY node scripts/bake-off.mts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd proxy && env -u GEMINI_API_KEY -u ANTHROPIC_API_KEY node scripts/bake-off.mts` prints a skip message naming GEMINI_API_KEY and ANTHROPIC_API_KEY and exits 0
    - `grep -F "claude-sonnet-5" proxy/scripts/bake-off.mts` exits 0 (hybrid verdict candidate in the default matrix)
    - `grep -F "from \"../src/providers/" proxy/scripts/bake-off.mts` (or equivalent relative import) exits 0 — script imports production adapters, no duplicated wire logic
    - `grep -c "Should I" proxy/scripts/bake-off.mts` returns a number ≥ 15 (20 inline dilemmas present)
    - `grep -F "bake-off" proxy/package.json` exits 0
    - `cd proxy && npx vitest run` still exits 0 (no regression)
  </acceptance_criteria>
  <done>Bake-off tooling committed and syntax-verified offline; runnable in one command the day ANTHROPIC_API_KEY is set; exercises production adapters against 20 fixed dilemmas with a latency + mechanical-quality table.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator sets up Cloudflare auth and the fresh Gemini secret</name>
  <read_first>
    - .planning/phases/01-backend-gemini-proxy/01-RESEARCH.md (section "Secrets & Local-Dev Workflow")
  </read_first>
  <what-built>The Worker from plans 01-01/01-02 is fully built and offline-tested. It cannot be deployed or hit real Gemini until you (the operator) authenticate wrangler and store a fresh API key. These steps involve browser OAuth and pasting a secret — Claude never handles the key value (the last key died by being pasted in chat; locked decision: launch with a fresh key).</what-built>
  <how-to-verify>
    Run in YOUR terminal, from the gordian repo root:
    1. If you have no Cloudflare account: create a free one at dash.cloudflare.com.
    2. `cd proxy && npx wrangler login` — completes browser OAuth.
    3. Create a NEW Gemini API key at aistudio.google.com (do NOT reuse the spike-era key — it is compromised and should be deleted/rotated there while you're in the console).
    4. `npx wrangler secret put GEMINI_API_KEY` — paste the new key at the hidden prompt. If wrangler asks to create a new Worker named "gordian-proxy", answer yes.
    5. Optional (enables local `wrangler dev` + `npm run test:live` against localhost): create `proxy/.dev.vars` containing `GEMINI_API_KEY="<your key>"` — the file is gitignored.
    6. ANTHROPIC_API_KEY: skip for now — deferred until you run the bake-off (`npx wrangler secret put ANTHROPIC_API_KEY` then, plus the env var locally).
  </how-to-verify>
  <acceptance_criteria>
    - `cd proxy && npx wrangler whoami` exits 0 and prints the account
    - `cd proxy && npx wrangler secret list` output includes the name GEMINI_API_KEY (names only — values are write-only)
    - `git -C . status --porcelain` shows no `.dev.vars` file staged/untracked-and-committed (gitignore holding)
  </acceptance_criteria>
  <resume-signal>Type "done" once wrangler login and the GEMINI_API_KEY secret are set</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy to workers.dev and live-smoke both endpoints</name>
  <files>proxy/wrangler.jsonc (conditional — only if the model-name 404 fallback flip to gemini-flash-latest is needed)</files>
  <read_first>
    - proxy/scripts/live-check.mjs (the smoke script from plan 01-02, reused here against the deployed URL)
    - proxy/wrangler.jsonc (worker name and vars being deployed)
  </read_first>
  <action>
    From proxy/: run `npx wrangler deploy` and capture the printed workers.dev URL (expected shape: https://gordian-proxy.<subdomain>.workers.dev). Then smoke the deployment with curl using a freshly generated UUID as X-Device-ID: (1) POST {URL}/v1/session-plan with body {"scenario":"Should I learn Spanish or German?"} — expect HTTP 200, JSON containing mode/optionA/optionB and a questions array of 5-15 strings, and an X-Weekly-Sessions response header; (2) POST {URL}/v1/verdict with that scenario plus 3 canned answers — expect HTTP 200 with non-empty decision/sentiment/analysis/probe strings; (3) POST /v1/session-plan with NO X-Device-ID header — expect HTTP 400 with error.code "bad_request"; (4) GET {URL}/v1/guides — expect HTTP 404 with error.code "not_found". Then run `GORDIAN_LIVE=1 BASE_URL={URL} node scripts/live-check.mjs` — expect exit 0. If Gemini 404s on the model name, flip MODEL_SESSION_PLAN/MODEL_VERDICT vars to "gemini:gemini-flash-latest" in wrangler.jsonc (the conditional edit this task's files entry declares), redeploy, and note it. Expect ~7s latency per generation call (thinking model) — that is normal, not a hang.

    Record in the SUMMARY, for the user (decisions deferred by design — notes only, do NOT act on them): (a) the deployed workers.dev URL, and that a custom domain (requires a domain on a Cloudflare zone) should be decided before Phase 3 bakes the URL into the app binary — some networks filter workers.dev; (b) App Attest/DeviceCheck hardening is the designed follow-up lane (X-Device-ID tier hook already in DeviceMeterDO) — a post-launch phase, deliberately not built now; (c) ANTHROPIC_API_KEY + `npm run bake-off` are ready whenever the operator wants the model bake-off.
  </action>
  <verify>
    <automated>cd proxy && GORDIAN_LIVE=1 BASE_URL=$(npx wrangler deploy 2>/dev/null | grep -o 'https://[^ ]*workers.dev' | head -1) node scripts/live-check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx wrangler deploy` exits 0 and prints a workers.dev URL
    - curl POST {URL}/v1/session-plan (valid UUID header + scenario) returns HTTP 200 and the body contains the strings "mode", "optionA", "optionB", "questions"
    - curl POST {URL}/v1/verdict returns HTTP 200 and the body contains "decision", "sentiment", "analysis", "probe"
    - curl POST {URL}/v1/session-plan without X-Device-ID returns HTTP 400 and body contains "bad_request"
    - curl GET {URL}/v1/guides returns HTTP 404
    - `GORDIAN_LIVE=1 BASE_URL={URL} node scripts/live-check.mjs` exits 0
    - SUMMARY records the deployed URL + the custom-domain and App Attest deferral notes
  </acceptance_criteria>
  <done>Live proxy on workers.dev serving both operations from real Gemini with the server-held fresh key; error-code contract verified against production; deferred decisions recorded for the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator terminal → Cloudflare | OAuth token + write-only secrets; Claude never touches key values |
| Internet → deployed workers.dev URL | Public endpoint; anyone who scrapes the URL can call it |
| Bake-off script → provider APIs | Keys from operator's local env only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information disclosure | GEMINI_API_KEY handling | mitigate | `wrangler secret put` in operator's own terminal (write-only after set); fresh key replaces the compromised spike key; `.dev.vars*` gitignored (verified in Task 2 acceptance); key never appears in chat, code, or plan output |
| T-01-07 | Spoofing/DoS | Public workers.dev endpoint replayed by non-app clients | accept (launch) | Blast radius dollar-bounded by SpendCapDO + per-device limits from 01-02; App Attest hardening is the documented post-launch phase; honeypot logging (hashed ids hitting limits) available in Workers Logs |
| T-01-08 | Elevation of privilege | wrangler OAuth token on operator machine | accept | Standard Cloudflare CLI posture; scoped to the operator's free account; no CI tokens created this phase |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (bake-off uses Node built-ins + existing src imports); 01-01's audited dev deps unchanged |
</threat_model>

<verification>
- Deployed URL answers all four curl smokes with the exact expected status + code strings
- `GORDIAN_LIVE=1 BASE_URL={URL} node scripts/live-check.mjs` exits 0 against production
- `env -u GEMINI_API_KEY -u ANTHROPIC_API_KEY node scripts/bake-off.mts` exits 0 with a skip notice (offline-safe)
- `npx wrangler secret list` shows GEMINI_API_KEY by name; no secret value ever entered Claude's context
</verification>

<success_criteria>
- REQ-001 complete: a first-party deployed service holds the (fresh) Gemini key; the app-side contract needs zero user setup
- REQ-006 proven live: bad_request and not_found observable on production; rate_limited/spend_cap proven in the 01-02 suite; ~7s latency confirmed compatible with the app's loading states
- Bake-off runnable in one command once ANTHROPIC_API_KEY exists; per-operation model swap needs only a var change + redeploy, never an app release
- Deferred items recorded, not built: custom domain decision, App Attest hardening, Anthropic key, guides.json content
</success_criteria>

<output>
Create `.planning/phases/01-backend-gemini-proxy/01-03-SUMMARY.md` when done. Must record the deployed workers.dev URL, the model vars deployed, and the three deferral notes (custom domain before Phase 3, App Attest follow-up, bake-off pending ANTHROPIC_API_KEY).
</output>
