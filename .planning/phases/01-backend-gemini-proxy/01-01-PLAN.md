---
phase: 01-backend-gemini-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proxy/package.json
  - proxy/wrangler.jsonc
  - proxy/tsconfig.json
  - proxy/vitest.config.ts
  - proxy/.gitignore
  - proxy/src/index.ts
  - proxy/src/operations.ts
  - proxy/src/providers/types.ts
  - proxy/src/providers/gemini.ts
  - proxy/src/providers/anthropic.ts
  - proxy/test/fixtures/response-questions.json
  - proxy/test/fixtures/response-verdict.json
  - proxy/test/fixtures/response-error.json
  - proxy/test/fixtures/response-truncated.json
  - proxy/test/gemini.test.ts
autonomous: false
requirements: [REQ-001, REQ-005]
user_setup:
  - service: gemini
    why: "Upstream LLM — fresh key set in plan 01-03 via wrangler secret put (spike key is compromised)"
    env_vars:
      - name: GEMINI_API_KEY
        source: "aistudio.google.com — set in plan 01-03, NOT this plan; offline tests need no key"

must_haves:
  truths:
    - "Both system prompts (session-plan classification incl. YES_NO/BINARY modes + 12-question spec; verdict incl. decision/sentiment/analysis/probe field spec) live server-side, ported verbatim from SessionViewModel.swift"
    - "The Gemini adapter sets responseSchema on every call, concatenates ALL response parts, and repairs truncated JSON before failing"
    - "Truncated Gemini JSON (missing closing bracket, finishReason STOP) is repaired and served, not failed"
    - "Spike-002 fixtures replay green through the Gemini adapter parse/repair path"
    - "The Anthropic adapter builds a Messages API request with required max_tokens and output_config.format json_schema"
    - "The proxy has zero runtime dependencies (raw fetch only; wrangler/vitest/pool-workers/typescript as devDependencies)"
  artifacts:
    - path: "proxy/src/operations.ts"
      provides: "Both system prompts (ported verbatim from SessionViewModel.swift) + 4 schema constants (Gemini uppercase + Anthropic lowercase dialects for both operations)"
      contains: "rapid gut-instinct bypass"
    - path: "proxy/src/providers/gemini.ts"
      provides: "generateContent adapter: responseSchema, x-goog-api-key header, parts concatenation, bracket-repair ladder"
      contains: "generativelanguage.googleapis.com"
    - path: "proxy/src/providers/anthropic.ts"
      provides: "Messages API adapter with output_config.format json_schema and required max_tokens"
      contains: "api.anthropic.com"
    - path: "proxy/src/providers/types.ts"
      provides: "Provider interface, OperationConfig parser for '{provider}:{model}' vars, error-code union incl. reserved weekly_meter_exhausted"
      contains: "weekly_meter_exhausted"
    - path: "proxy/test/gemini.test.ts"
      provides: "Fixture-replay tests for parse/repair path"
  key_links:
    - from: "proxy/src/providers/gemini.ts"
      to: "generativelanguage.googleapis.com"
      via: "x-goog-api-key header (never ?key= query param)"
      pattern: "x-goog-api-key"
    - from: "proxy/src/operations.ts"
      to: "ios/Gordian/SessionViewModel.swift"
      via: "prompts ported verbatim"
      pattern: "12 rapid-fire"
---

<objective>
Scaffold the Gordian proxy Worker in `proxy/` at the gordian repo root and build its generation core: server-side prompts (ported verbatim from the iOS app), the four schema constants, and both provider adapters (Gemini live path with the spike-proven repair ladder; Anthropic bake-off path), proven by a fixture-replay unit suite. Routing, meters, and deployment come in plans 01-02 and 01-03.

Purpose: REQ-001 (key server-side, prompts server-side so prompt iteration needs no app release), REQ-005 (responseSchema + parts concat + repair ladder move server-side).
Output: a `proxy/` TypeScript scaffold with zero runtime dependencies that passes `npx tsc --noEmit` and `npx vitest run test/gemini.test.ts` offline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

Working directory for all commands: the gordian repo root (`/Users/ashwink/Desktop/Claude Projects/gordian`), with the Worker in `proxy/`.

@.planning/phases/01-backend-gemini-proxy/01-RESEARCH.md
@.planning/phases/01-backend-gemini-proxy/01-CONTEXT.md
@.claude/skills/spike-findings-gordian/references/gemini-structured-output.md
@ios/Gordian/SessionViewModel.swift
@ios/Gordian/GeminiClient.swift

<interfaces>
<!-- The wire contracts this plan implements. Executor uses these directly. -->

**Worker vars (wrangler.jsonc `vars`, all tunable per deploy, defaults shown):**

```
MODEL_SESSION_PLAN = "gemini:gemini-3.5-flash"     // "{provider}:{model}", split on first ":"
MODEL_VERDICT      = "gemini:gemini-3.5-flash"     // bake-off flip: "anthropic:claude-haiku-4-5" / "anthropic:claude-sonnet-5"
DAILY_CALL_CAP     = "1500"                        // global upstream calls/day (UTC), SpendCapDO (plan 01-02)
RATE_LIMIT_CALLS   = "6"                           // per device, sliding window (plan 01-02)
RATE_LIMIT_WINDOW_MIN = "10"
DEVICE_DAILY_SESSIONS = "20"                       // per device per UTC day (session-plan calls)
MAX_SCENARIO_CHARS = "500"
```
Secrets (NOT set this plan): GEMINI_API_KEY, ANTHROPIC_API_KEY.

**Error-code union (defined here in types.ts; the plan 01-02 router returns them):**
`"bad_request" | "rate_limited" | "weekly_meter_exhausted" | "spend_cap" | "upstream_error" | "not_found"` — weekly_meter_exhausted is RESERVED: defined in the union, never returned this phase (Phase 3.5 freemium meter).

**Gemini upstream (spike-proven contract — do NOT migrate to the newer Interactions API, see RESEARCH Pitfall 4):**

```
POST https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent
Header: x-goog-api-key: {GEMINI_API_KEY}          // header, NEVER ?key= query param
Body: { systemInstruction: {parts:[{text: system}]},
        contents: [{parts:[{text: user}]}],
        generationConfig: { temperature: 0.8, responseMimeType: "application/json",
                            responseSchema: <uppercase OpenAPI-style schema> } }
// Do NOT set maxOutputTokens (thinking model burns ~2000 thought tokens).
// Extract: concatenate ALL candidates[0].content.parts[].text — never parts[0] alone.
// Repair ladder (port line-for-line from GeminiClient.swift cleanJSONText + bracket repair):
//   strip ```json / ``` fences → trim trailing comma → append missing "]" or "}" → JSON.parse
// Non-2xx decodes as { error: { code, message, status } } → surface message, map to upstream_error.
```

**Anthropic upstream (mock-tested only this phase; live in the plan 01-03 bake-off):**

```
POST https://api.anthropic.com/v1/messages
Headers: x-api-key: {ANTHROPIC_API_KEY}, anthropic-version: 2023-06-01, content-type: application/json
Body: { model, max_tokens: 1024 /* REQUIRED — omitting it 400s */, system,
        messages: [{role:"user", content: user}],
        output_config: { format: { type: "json_schema",
          schema: <lowercase JSON Schema, additionalProperties:false> } } }
// Result JSON text: response.content[0].text — schema-valid by constrained decoding; NO repair ladder.
```

**Schemas (4 hand-written constants in operations.ts — no schema translator):**
- session-plan / Gemini: `{type:"OBJECT", properties:{mode:{type:"STRING"}, optionA:{type:"STRING"}, optionB:{type:"STRING"}, questions:{type:"ARRAY", items:{type:"STRING"}}}, required:["mode","optionA","optionB","questions"]}`
- verdict / Gemini: `{type:"OBJECT"}` with decision, sentiment, analysis, probe all `{type:"STRING"}`, all required
- session-plan / Anthropic and verdict / Anthropic: lowercase JSON Schema equivalents with `"additionalProperties": false`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm vitest package legitimacy before install</name>
  <read_first>
    - .planning/phases/01-backend-gemini-proxy/01-RESEARCH.md (section "Package Legitimacy Audit" only)
  </read_first>
  <what-built>Nothing yet — this gate precedes the npm install in Task 2. The research package audit (slopcheck 0.6.1, 2026-07-20) approved wrangler, @cloudflare/vitest-pool-workers, and typescript, but flagged `vitest` as [SUS] via a typosquat-proximity heuristic ("close to 'vite'"). The researcher assessed it a false positive (official vitest-dev/vitest repo, published since 2021-12, `npm view` provenance verified), but [SUS] packages require human sign-off before install per the package legitimacy gate. This checkpoint is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest in your browser.
    2. Confirm the repository link points to github.com/vitest-dev/vitest and weekly downloads are in the millions.
    3. That's it — the other three dev deps (wrangler, @cloudflare/vitest-pool-workers, typescript) passed slopcheck [OK].
  </how-to-verify>
  <acceptance_criteria>
    - User has confirmed the npmjs.com/package/vitest page shows repo vitest-dev/vitest
    - User typed approval to proceed with `npm i -D wrangler vitest @cloudflare/vitest-pool-workers typescript`
  </acceptance_criteria>
  <resume-signal>Type "approved" to unblock the install, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold proxy/, port prompts + schemas, build both provider adapters, fixture-replay unit tests</name>
  <files>proxy/package.json, proxy/wrangler.jsonc, proxy/tsconfig.json, proxy/vitest.config.ts, proxy/.gitignore, proxy/src/index.ts, proxy/src/operations.ts, proxy/src/providers/types.ts, proxy/src/providers/gemini.ts, proxy/src/providers/anthropic.ts, proxy/test/fixtures/response-questions.json, proxy/test/fixtures/response-verdict.json, proxy/test/fixtures/response-error.json, proxy/test/fixtures/response-truncated.json, proxy/test/gemini.test.ts</files>
  <read_first>
    - .claude/skills/spike-findings-gordian/references/gemini-structured-output.md (wire contract + truncation trap)
    - ios/Gordian/GeminiClient.swift (repair ladder to port line-for-line: cleanJSONText + bracket repair in generateStringArray/generateObject)
    - ios/Gordian/SessionViewModel.swift lines 226-280 and 403-471 (the two prompts, verbatim source; the rapid-fire Q&A line format; "None (User was silent during rapid-fire)" empty-case string)
    - .planning/spikes/002-gemini-swift-client/fixtures/ (the three real captured responses to copy)
    - .planning/phases/01-backend-gemini-proxy/01-RESEARCH.md (sections "Provider Adapter Design", "Standard Stack", "Common Pitfalls")
  </read_first>
  <action>
    Scaffold `proxy/` at the gordian repo root. `package.json`: private, type module, scripts {"test": "vitest run", "test:live": "node scripts/live-check.mjs", "dev": "wrangler dev", "deploy": "wrangler deploy"}; devDependencies ONLY (wrangler ^4.112.0, vitest ^4.1.10, @cloudflare/vitest-pool-workers ^0.18.6, typescript ~5.x); zero runtime dependencies — runtime code uses raw fetch. Run `npm install` (approved in Task 1). `.gitignore`: `.dev.vars*`, `node_modules/`, `dist/`, `.wrangler/`, `scripts/bake-off-results/`. `wrangler.jsonc`: name "gordian-proxy", main "src/index.ts", a current compatibility_date, and the seven `vars` from the interfaces block with their defaults (MODEL_SESSION_PLAN, MODEL_VERDICT, DAILY_CALL_CAP, RATE_LIMIT_CALLS, RATE_LIMIT_WINDOW_MIN, DEVICE_DAILY_SESSIONS, MAX_SCENARIO_CHARS). DO bindings are added in plan 01-02. `vitest.config.ts` uses defineWorkersConfig from @cloudflare/vitest-pool-workers/config pointing at wrangler.jsonc. `src/index.ts` starts as a minimal fetch handler returning the 404 not_found envelope (plan 01-02 replaces it with the real router) so config validation and the test pool boot.

    `src/operations.ts` — the two operations as data. For each operation export: a system-prompt builder, the fixed user message, temperature 0.8, and both schema dialects. Port the prompts VERBATIM from SessionViewModel.swift, with the scenario interpolated server-side: (a) sessionPlanSystem(scenario) is the string built in generateBypassQuestionsAndStart — "You are an expert cognitive psychologist specializing in rapid gut-instinct bypass. The user has a dilemma: '{scenario}'.\n" plus the STEP 1 classification paragraph (BINARY with 1-3-word Title Case optionA/optionB extraction vs YES_NO with optionA='No' optionB='Yes'), the STEP 2 exactly-12 rapid-fire questions spec (max 12 words each, instantly tappable, BINARY vs YES_NO phrasing rules), and the closing Return-JSON line naming mode/optionA/optionB/questions; user message exactly "Classify the dilemma and generate the 12 bypass questions as JSON.". (b) verdictSystem(scenario, answers) is the string built in evaluateFullSessionAndLog: reconstruct rfText server-side from the answers array using the exact line format `"{n}. Q: {question} -> Response: {choice} {reflection.isEmpty ? "" : "(Reflection: {reflection})"}"` joined with newlines, and exactly "None (User was silent during rapid-fire)" when answers is empty; then the full verdict prompt with the four numbered field specs (decision ≤15 words naming the winner; sentiment single short affective state; analysis 2-3 plain concrete sentences referencing actual responses; probe one concrete instruction ≤15 words) and the Output-ONLY-JSON closing; user message exactly "Synthesize a final Gordian Verdict and return JSON.". Copy the prompt text character-for-character from the Swift source (converting Swift string concatenation/escapes to TS template literals) — do not paraphrase. Also export the 4 schema constants from the interfaces block.

    `src/providers/types.ts` — Provider interface with generateStructured(req, env) returning Promise of JSON text; StructuredRequest carries {system, user, geminiSchema, anthropicSchema, temperature, maxTokens}; OperationConfig parsed from a "{provider}:{model}" var string (split on first ":", reject unknown providers). Export the error-code union type: "bad_request" | "rate_limited" | "weekly_meter_exhausted" | "spend_cap" | "upstream_error" | "not_found" — weekly_meter_exhausted is defined but never returned this phase (Phase 3.5 freemium meter reserves it). CONSTRAINT: providers/*.ts and operations.ts must import nothing workerd-specific (no cloudflare:workers) — plan 01-03's bake-off script imports them directly under Node's native TypeScript type-stripping.

    `src/providers/gemini.ts` — implement the interfaces-block contract exactly: generateContent URL with the model from config, x-goog-api-key header (never a ?key= query param — the spike/client style is a known anti-pattern server-side), systemInstruction + contents + generationConfig with temperature, responseMimeType "application/json", responseSchema; no maxOutputTokens. Concatenate ALL parts text. Then the repair ladder ported line-for-line from GeminiClient.swift: trim; strip leading ```json or ``` and trailing ```; trim again; JSON.parse; on failure drop a trailing comma, append "]" if it starts with "[" without closing, append "}" if it starts with "{" without closing, retry parse; still failing → throw a typed UpstreamError. Non-2xx: decode the {error:{code,message,status}} envelope, throw UpstreamError with the message (never the raw body).

    `src/providers/anthropic.ts` — Messages API per the interfaces block: x-api-key + anthropic-version headers, max_tokens 1024 explicitly (Pitfall 5: omitting it 400s), system, single user message, output_config.format json_schema with the lowercase schema. Return response.content[0].text directly — constrained decoding guarantees validity, no repair ladder on this path. Missing ANTHROPIC_API_KEY in env → typed UpstreamError with a clear message.

    Copy the three spike fixtures from .planning/spikes/002-gemini-swift-client/fixtures/ into proxy/test/fixtures/ unchanged, and author response-truncated.json: a synthetic generateContent 200 body whose parts text is a session-plan JSON object missing its closing brace (finishReason "STOP") — this pins the ~40% truncation failure mode the ladder exists for.

    `test/gemini.test.ts` — replay fixtures through the Gemini adapter parse/repair path (mock fetch or call the exported parse/repair functions directly): (1) response-questions.json and response-verdict.json parse to valid JSON; (2) a multi-part response (split one fixture's text across two parts) concatenates correctly; (3) fenced ```json output is stripped; (4) trailing comma is repaired; (5) response-truncated.json is repaired to a parseable object; (6) response-error.json maps to an UpstreamError carrying the upstream message; (7) irreparable garbage throws UpstreamError. Also assert both Gemini schema constants set responseSchema fields matching the required lists (REQ-005 guard) and the Anthropic adapter builds a body containing max_tokens and output_config.format.type "json_schema" (assert via mocked fetch capture).
  </action>
  <verify>
    <automated>cd proxy && npx tsc --noEmit && npx vitest run test/gemini.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd proxy && npx tsc --noEmit` exits 0
    - `cd proxy && npx vitest run test/gemini.test.ts` exits 0 with all tests passing
    - `grep -c "dependencies" proxy/package.json` shows devDependencies only (no runtime "dependencies" key)
    - `grep -F "x-goog-api-key" proxy/src/providers/gemini.ts` exits 0 and `grep -F "?key=" proxy/src/providers/gemini.ts` exits 1
    - `grep -F "rapid gut-instinct bypass" proxy/src/operations.ts` exits 0 (session-plan prompt ported)
    - `grep -F "None (User was silent during rapid-fire)" proxy/src/operations.ts` exits 0 (verdict empty-answers string ported)
    - `grep -F "max_tokens" proxy/src/providers/anthropic.ts` exits 0
    - `grep -F "weekly_meter_exhausted" proxy/src/providers/types.ts` exits 0 (reserved code defined)
    - proxy/test/fixtures/ contains response-questions.json, response-verdict.json, response-error.json, response-truncated.json
    - `grep -F ".dev.vars" proxy/.gitignore` exits 0
  </acceptance_criteria>
  <done>Proxy scaffold compiles; both adapters implemented against the spike-proven contracts; prompts live server-side verbatim; fixture-replay unit suite green offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Gemini/Anthropic | Operator's API key attached server-side; user text passes through |
| Dev machine → npm registry | Dev-dependency installs cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Prompt injection via scenario text | mitigate | Prompts are server-side constants; responseSchema/constrained decoding bounds output shape; output parsed as JSON, never executed (input length caps enforced by plan 01-02's validate.ts) |
| T-01-05 | Information disclosure | API key in URLs/log lines | mitigate | x-goog-api-key header (never ?key= query param); keys only via env bindings; grep gate in acceptance criteria |
| T-01-SC | Tampering | npm installs (wrangler, vitest, pool-workers, typescript) | mitigate | Research-phase slopcheck audit + blocking human-verify checkpoint (Task 1) for the [SUS]-flagged vitest before install; zero runtime dependencies by design |
</threat_model>

<verification>
- `cd proxy && npx tsc --noEmit` — clean typecheck
- `cd proxy && npx vitest run test/gemini.test.ts` — fixture replay + repair ladder + adapter body-shape tests green offline
- Grep gates: prompts ported verbatim (sentinel strings present), no ?key= usage, reserved error code defined, .dev.vars gitignored
</verification>

<success_criteria>
- Spike-002 fixtures replay green through the Gemini adapter parse/repair path, including the synthetic truncated fixture
- Both operations' prompts and all four schema constants live server-side, character-faithful to SessionViewModel.swift
- Zero runtime dependencies; secrets untouched (plan 01-03 owns them); no routing/meters yet (plan 01-02 owns them)
</success_criteria>

<output>
Create `.planning/phases/01-backend-gemini-proxy/01-01-SUMMARY.md` when done. Record: file inventory, test counts, and the note that routing/meters land in plan 01-02 and GEMINI_API_KEY/deploy in plan 01-03.
</output>
