{
  "name": "gordian-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:live": "node scripts/live-check.mjs",
    "bake-off": "node scripts/bake-off.mts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "typescript": "~5.9.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0"
  }
}
